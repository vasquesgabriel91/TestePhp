# TestePhp
Para rodar:
docker compose build
docker compose up -d

dentro da pasta laravel :
composer install

rodar migrations acessar o container 
docker exec -it laravel_app bash

depois

php artisan migrate